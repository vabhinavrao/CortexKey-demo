╔════════════════════════════════════════════════════════════╗
║          ESP32 UPLOAD & TEST QUICK REFERENCE               ║
╚════════════════════════════════════════════════════════════╝

🔌 YOUR ESP32 PORT DETECTED: /dev/cu.usbserial-0001

┌────────────────────────────────────────────────────────────┐
│ STEP 1: UPLOAD FIRMWARE (Do this in Arduino IDE)          │
└────────────────────────────────────────────────────────────┘

  1. Open Arduino IDE
  2. File → Open → firmware/esp32_neural_auth_v2.ino
  3. Tools → Board → "ESP32 Dev Module"
  4. Tools → Port → "/dev/cu.usbserial-0001"
  5. Click Upload (→) button
  6. Wait for "Done uploading"

┌────────────────────────────────────────────────────────────┐
│ STEP 2: MONITOR SERIAL OUTPUT (Choose one method)         │
└────────────────────────────────────────────────────────────┘

  METHOD A: Arduino IDE Serial Monitor
  ─────────────────────────────────────
  • Tools → Serial Monitor
  • Set baud: 115200
  • Press EN button on ESP32
  • Should see startup banner

  METHOD B: Python Serial Monitor (Better, with colors!)
  ───────────────────────────────────────────────────────
  Terminal command:
  
  cd /Users/abhinavrao/coding/hackathon/demo/cortexkey
  python tools/esp32_monitor.py

  This will show:
  • ✅ Colored output
  • 📊 Live statistics
  • ⏱️ Timestamps
  • 🎨 Easy-to-read formatting

┌────────────────────────────────────────────────────────────┐
│ STEP 3: TEST BUTTONS                                       │
└────────────────────────────────────────────────────────────┘

  🟢 BUTTON 1 (GPIO18) - Valid User Test
  ───────────────────────────────────────
  Expected output:
  ========== VALID USER TEST STARTED ==========
  STATUS:Button 18 pressed - Starting valid user authentication
  DATA,0,24.531
  DATA,4,26.872
  ... (clean, regular pattern)
  ========== TEST COMPLETE (10s) ==========
  
  Result: HIGH CONFIDENCE → AUTHENTICATED ✅

  🔴 BUTTON 2 (GPIO19) - Invalid User Test
  ────────────────────────────────────────
  Expected output:
  ========== INVALID USER TEST STARTED ==========
  STATUS:Button 19 pressed - Starting invalid user authentication
  DATA,0,48.234
  DATA,4,-12.543
  ... (noisy, irregular pattern)
  ========== TEST COMPLETE (10s) ==========
  
  Result: LOW CONFIDENCE → DENIED ❌

  ⏸️  LONG PRESS (2 seconds) - Stop Test
  ──────────────────────────────────────
  ========== TEST STOPPED ==========
  STATUS:Long press detected - Returned to idle

┌────────────────────────────────────────────────────────────┐
│ HARDWARE CONNECTIONS (Double-check!)                       │
└────────────────────────────────────────────────────────────┘

  ESP32          Connection
  ──────────────────────────────────────
  GPIO18    →    [Button 1] → GND  (Valid user)
  GPIO19    →    [Button 2] → GND  (Invalid user)
  GPIO2     →    Built-in LED (auto)
  GND       →    Common ground

  Note: Internal pull-ups are enabled in firmware,
        no external resistors needed!

┌────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                            │
└────────────────────────────────────────────────────────────┘

  ❌ "Port not found"
  → Check: ls /dev/cu.* | grep usb
  → Install CP2102 driver if needed

  ❌ "Failed to connect" during upload
  → Hold BOOT button, click Upload, release when connecting

  ❌ Garbage characters in serial monitor
  → Check baud rate is 115200
  → Press EN button on ESP32

  ❌ Buttons not responding
  → Verify wiring (GPIO to button to GND)
  → Test button with multimeter
  → Send serial command: "STATUS"

┌────────────────────────────────────────────────────────────┐
│ FILES READY FOR YOU                                        │
└────────────────────────────────────────────────────────────┘

  ✅ firmware/esp32_neural_auth_v2.ino  (Ready to upload)
  ✅ tools/esp32_monitor.py             (Colored serial monitor)
  ✅ ESP32_BUTTON_TESTING_GUIDE.md      (Detailed guide)
  ✅ UPLOAD_ESP32.md                    (Step-by-step upload)

┌────────────────────────────────────────────────────────────┐
│ QUICK COMMANDS                                             │
└────────────────────────────────────────────────────────────┘

  # Check if ESP32 is connected
  ls /dev/cu.* | grep usb

  # Monitor serial output (Python, with colors)
  python tools/esp32_monitor.py

  # Run full authentication test
  cd CortexKey-Python
  python brainwave_auth.py --port /dev/cu.usbserial-0001

╔════════════════════════════════════════════════════════════╗
║  🚀 YOU'RE READY TO UPLOAD AND TEST!                      ║
║                                                            ║
║  1. Open Arduino IDE                                       ║
║  2. Upload firmware/esp32_neural_auth_v2.ino              ║
║  3. Run: python tools/esp32_monitor.py                    ║
║  4. Press buttons and watch the magic! ✨                 ║
╚════════════════════════════════════════════════════════════╝
